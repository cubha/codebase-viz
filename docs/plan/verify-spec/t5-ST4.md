### VERIFY-SPEC — SubTask ST4

- 기준선 요구사항: "배지 툴팁 키 1개 4로케일(ko/en/ja/zh-cn). 키 배치는 파일 규칙(그룹 내 UI 표시 순)
  준수."
- 변경 파일: `packages/extension/src/i18n/dict.ts`
- 관찰 가능한 계약: `db.ormClass` 키가 KO/EN/JA/ZH 4블록 전부에 존재하고, 각 블록에서
  `db.sidebar.tables` 바로 뒤(= `db.*` 그룹 내)에 위치한다.
- 구현 결정:
  - 문구를 "ORM 클래스명 (엔티티 선언에서 추론)" 형태로 잡아 **추론 결과임을 명시**했다(제약 5,
    Evidence-First). "이 테이블은 이 클래스다"라는 단정문을 피한다.
  - 배치는 알파벳순이 아니라 `db.*` 그룹 내 UI 표시 순 — v1.2.66에서 이 파일의 실제 컨벤션으로
    주석을 교정한 그 규칙을 따랐다.
- 인접 경계: `tr()` 폴백(`FALLBACK_LOCALE='en'`)은 키가 4로케일에 다 있어 발동하지 않는다.
- 미확인 사항: ja/zh 번역은 1차 번역 품질이다(파일 상단 주석이 밝힌 기존 상태와 동일 수준).
  네이티브 검수를 받지 않았다.

{
  "name": "codebase-viz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "lint": "oxlint --config .oxlintrc.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.0.0",
    "oxlint": "^1.74.0",
    "playwright": "^1.60.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0"
  }
}
